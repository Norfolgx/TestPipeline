pipeline {
  agent any
  //parameters {}
  environment {
    PATH = "/usr/local/bin:/bin:/usr/bin:/usr/local/sbin:/usr/sbin:/sbin:/opt/aws/bin"
  }
  stages {
    stage('Stage 1') {
      steps {
        sh 'node --version'
        }
      }
  }
  //post {}
}
